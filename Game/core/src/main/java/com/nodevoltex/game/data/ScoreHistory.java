package com.nodevoltex.game.data;
import com.badlogic.gdx.utils.Array;

public class ScoreHistory {
    // This wrapper tells LibGDX to safely save an array of multiple plays!
    public Array<SaveData> plays = new Array<>();
}
